# Test-only dependencies -- NOT installed in the production Docker
# image (backend/Dockerfile only installs requirements.txt). Splitting
# these out is what caught and fixed a real bug: schemathesis==3.31.2
# was pinned here but was never actually published to PyPI (it jumps
# 3.31.1 -> 3.32.0), which broke the production Render deploy build
# entirely -- for a package the running app never even imports.
# Keeping test tooling out of the deploy image means a bad test-dep
# pin like that can never again take down a real deploy.
-r requirements.txt
pytest==8.3.5
pytest-cov==5.0.0
schemathesis==3.31.1
moto[s3]==5.2.2
locust==2.46.2
