import DocumentListTab from "./DocumentListTab";

/** Documents (item 6): every uploaded document for this project. */
export default function DocumentsTab() {
  return <DocumentListTab emptyHint="Contracts, reports, and other project documents will appear here once uploaded." />;
}
